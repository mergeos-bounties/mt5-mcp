# mt5-mcp — mock-only env example
# Copy to .env and adjust.
# NEVER commit live broker secrets to git.

MT5_MCP_MODE=mock
MT5_MCP_MAGIC=202401
MT5_MCP_MAX_VOLUME=10.0
MT5_MCP_SYMBOL_ALLOWLIST=EURUSD,GBPUSD
